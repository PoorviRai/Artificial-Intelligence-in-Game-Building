(This folder contains the java and class files for the implementation of behavior tree.

The following classes are used for the implemetation of this part of the assignment: Graph, Edge, Node, ObjectPath, Character, Monster and BehaviorTree.
Graph, Edge, Node, ObjectPath and Character are the same as before.

Monster is the same as Character class with the addition of functions populate() for the scripted path to be followed and signatureMove().

BehaviorTree is the main class of this folder and contains the functions decisionTree() for the character and behaviorTree() for the monster as well as rangeCheck(), withinRange() and reset().
All the other functions are same as before.

The part for recording data and generating data.csv file has been commented out and can be uncommented to record new values.